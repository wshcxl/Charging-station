项目简介
这是一个基于嵌入式 Linux 系统的智能共享充电桩人机交互界面项目。

项目采用纯 C 语言开发，运行在 ARM 架构平台上。它通过直接操作 FrameBuffer 实现 LCD 液晶显示屏的图像渲染，
利用 Input 子系统处理触摸屏交互，并使用 Pthread 库实现触控检测的异步处理。
该项目实现了从开机动画、用户身份选择（商家/用户）到登录注册的完整业务流程。

技术栈
- 编程语言：C (C99 Standard)
- 核心库：Pthread (多线程), JPEG Library (图像解码)
- 硬件交互：
  - LCD (FrameBuffer)：负责显示驱动与显存操作
  - Touch Screen (Input Subsystem)：负责坐标采集
- 开发环境：ARM-Linux GCC 交叉编译工具链

系统架构与模块
本项目采用模块化设计，主程序 main.c 负责业务流程控制，具体功能模块如下：
模块文件   功能描述
main.c   系统入口，负责资源初始化、线程创建及UI状态机流转
lcd.c/h   显示驱动封装：封装了显存映射、清屏、像素点绘制及 JPEG 图片显示接口
ts.c/h   触控驱动封装：封装了触摸屏设备的打开、坐标读取及校准
ts_task.c/h   触控任务线程：独立线程运行，负责后台监听触控事件，避免阻塞主界面渲染
jpeg.c/h   图像解码：负责解析 JPEG 格式图片数据流并转换为显存格式
interfaceui.c   UI 业务逻辑：包含开机动画、选择界面、登录界面的绘制与交互逻辑

核心功能流程
1. 系统初始化：
   - 初始化 LCD 设备，映射显存。
   - 初始化触摸屏设备，获取文件描述符。
   - 创建 ts_task 子线程，独立处理触控输入。
2. 开机引导：
   - 显示开机动画（JPEG 解码显示）。
   - 清屏并进入主菜单。
3. 身份选择交互：
   - 绘制“商家”与“用户”选择区域。
   - 通过 SelectedLcd 函数检测点击坐标，判断用户意图。
4. 登录/注册业务：
   - 根据选择跳转至登录界面。
   - 处理登录或注册的 UI 状态跳转。
5. 资源释放：
   - 程序退出前自动关闭文件描述符，释放显存映射，确保无资源泄漏。

编译与运行
本项目使用 Makefile 构建脚本调用交叉编译工具链进行编译。

编译命令：
交叉编译命令示例
arm-linux-gcc main.c ./src/*.c -o ./bin/main -I ./include/ -L ./lib/ -ljpeg -lfont -lm -pthread

运行说明：
1. 将编译好的 main 可执行文件及依赖库（如 libjpeg.so）移植到 ARM 开发板。
2. 赋予执行权限：chmod +x main。
3. 确保设备节点 /dev/fb0 (LCD) 和 /dev/input/eventX (触摸屏) 存在且可读写。
4. 运行程序：./main。



核心难点与解决方案：

UI 渲染与触控响应的阻塞问题

痛点：在单线程嵌入式程序中，若采用“绘制->轮询触摸->绘制”的顺序结构，会导致界面刷新率低，且在执行耗时操作，如 JPEG 解码时完全无法响应点击。

解决方案：引入多线程并发模型。

主线程：专职负责“生产者”角色，处理 LCD 显存映射、JPEG 解码及界面绘制。

子线程：专职负责，后台死循环监听  /dev/input  节点。

同步机制：通过全局坐标结构体配合简单的状态标志位，实现触控事件与 UI 状态机的异步通信，确保界面流畅度与点击灵敏度的平衡。

显存直接操作与图像闪烁优化

利用  mmap  将显存地址映射到用户空间。

在内存中构建图像缓冲区，将 JPEG 解码后的数据通过  memcpy  一次性“搬运”至显存映射区。这种“双缓冲”思想的简化应用，有效消除了绘制过程中的撕裂与闪烁现象。





